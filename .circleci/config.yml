version: 2.1

orbs:
  aws-cli: circleci/aws-cli@4.1.2
  ansible-playbook: orbss/ansible-playbook@0.0.5

jobs:
  execute-cloud-formation:
    executor: aws-cli/default
    steps:
      - checkout
      - aws-cli/setup:
          aws_access_key_id: AWS_ACCESS_KEY_ID
          aws_secret_access_key: AWS_SECRET_ACCESS_KEY
          region: AWS_DEFAULT_REGION
      - run:
          name: Deploy cloudformation
          command: |
            set -x
            aws cloudformation deploy --template-file cloudformation/service.yml --stack-name sample-Stack --parameter-overrides CIDRIP=10.0.0.0/24 Prefix=RaiseTech --capabilities CAPABILITY_IAM

  execute-ansible:
    executor: ansible-playbook/default
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "SHA256:ZcPWdzOE2zdPrzEERDYkmgBJAHZwQgBDS4ZLSRHYUvM"
      - ansible-playbook/install:
            version: 2.10.7
      - ansible-playbook/playbook:
            playbook: ansible/playbook.yml
            inventory: ansible/inventory
            extra_vars: '{"user": "'${CircleCI_SSH_USER}'", "host": "'${CircleCI_TARGET_HOST}'"}'
workflows:
  RaiseTech:
    jobs:
      - execute-cloud-formation
      - execute-ansible:
         requires:
           - execute-cloud-formation